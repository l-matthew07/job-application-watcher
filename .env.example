# --- Slack ---
SLACK_WEBHOOK_URL=

# --- Generic webhook (Discord, ntfy, custom endpoint, etc.) ---
WEBHOOK_URL=

# --- Email / SMTP ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL_TO=
# --- X (Twitter) sentiment scraper ---
# App-only bearer token from the X developer portal. Recent search
# (GET /2/tweets/search/recent) requires a paid access tier.
X_BEARER_TOKEN=
